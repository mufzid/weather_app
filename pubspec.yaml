name: weather_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:

  cross_connectivity: ^3.1.0
  flutter:
    sdk: flutter
  gap: ^3.0.1
  http: ^1.2.0
  path: ^1.8.3
  provider: ^6.1.1
  sqflite: ^2.3.2



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
  - assets/images/
   
